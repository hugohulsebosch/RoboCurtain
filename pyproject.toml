[project]
name = "robocurtain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyswitchbot>=2.0.1",
    "requests>=2.33.1",
]

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["src"]